# CRMEB UI 自动化测试项目 — 完整说明文档

> **项目名称：** CRMEB 电商前端 UI 自动化测试项目
> **框架：** Playwright + pytest + Allure
> **设计模式：** 页面对象模型（Page Object Model, POM）
> **测试目标：** https://demo25.crmeb.net/ （CRMEB 演示站）
> **CI/CD：** GitHub Actions（自动测试 + 邮件通知）
> **文档版本：** v2.0
> **最后更新：** 2026 年 6 月 20 日

---

## 目录

- [1. 项目概述](#1-项目概述)
- [2. 环境要求](#2-环境要求)
- [3. 项目结构](#3-项目结构)
- [4. 快速开始](#4-快速开始)
- [5. 核心配置文件](#5-核心配置文件)
- [6. 公共工具模块（common/）](#6-公共工具模块common)
- [7. 测试数据（data/）](#7-测试数据data)
- [8. 页面对象层（page/）](#8-页面对象层page)
- [9. 测试用例层（script/）](#9-测试用例层script)
- [10. CI 辅助脚本（mes/）](#10-ci-辅助脚本mes)
- [11. GitHub Actions CI/CD](#11-github-actions-cicd)
- [12. 运行指南](#12-运行指南)
- [13. 自动化测试流程](#13-自动化测试流程)
- [14. 常见问题排查](#14-常见问题排查)
- [15. 已知问题与改进建议](#15-已知问题与改进建议)
- [附录：快速命令速查表](#附录快速命令速查表)

---

## 1. 项目概述

本项目是一个基于 **Playwright** 的端到端（E2E）UI 自动化测试项目，用于测试 **CRMEB** 电商系统前端的核心购物流程。项目采用 **Page Object Model（POM）** 设计模式，将页面元素定位与操作封装到独立的页面类中，测试用例只关注业务逻辑，从而提高代码的可维护性和可复用性。

项目还集成了 **GitHub Actions** 持续集成流水线，在代码推送或 PR 时自动执行测试，并通过 **SMTP 邮件通知** 将测试结果发送给相关人员。

### 核心测试流程

```
登录 → 搜索商品 → 点击查看商品 → 选择规格 + 加入购物车 → 进入购物车 → 去结算
```

### 技术栈

| 技术 | 用途 | 版本 |
|------|------|------|
| **Python** | 编程语言 | 3.9+（CI 使用 3.10） |
| **Playwright** | 浏览器自动化框架 | 1.60.0 |
| **pytest** | 测试框架 | 9.0.3 |
| **pytest-playwright** | pytest 与 Playwright 集成插件 | 0.8.0 |
| **Allure** | 测试报告生成 | 2.16.0 |
| **pytest-xdist** | 分布式并行测试 | 3.8.0 |
| **pytest-rerunfailures** | 失败用例自动重跑 | 16.3 |
| **Page Object Model** | 设计模式 | — |
| **JSON** | 测试数据存储 | — |
| **Logging** | 日志记录 | — |
| **GitHub Actions** | CI/CD 流水线 | — |
| **SMTP** | 邮件通知 | — |

---

## 2. 环境要求

### 本地开发环境

- **Python：** 3.9 或更高版本
- **操作系统：** Windows / macOS / Linux
- **浏览器：** Chromium（Playwright 内置，无需单独安装）
- **IDE 推荐：** PyCharm Professional 或 VS Code
- **Allure 命令行工具：** 用于生成 HTML 报告（可选）

### CI 环境

- **运行平台：** GitHub Actions（`ubuntu-latest`）
- **Python 版本：** 3.10
- **浏览器：** Chromium（CI 中自动安装）

---

## 3. 项目结构

### 完整目录树

```
E:/PY/project/crmeb/
├── .github/                            # GitHub Actions CI 配置
│   └── workflows/
│       └── ci.yml                      # CI 流水线定义（15 个步骤）
│
├── common/                             # 公共工具模块
│   ├── __init__.py                     # 包标识文件（空）
│   ├── log_config.py                   # 日志配置工具
│   └── read_login_json.py              # JSON 数据读取工具
│
├── data/                               # 测试数据目录
│   └── data_login.json                 # 登录测试数据（6 组场景）
│
├── log/                                # 日志输出目录
│   └── log_test.log                    # 运行时日志文件（自动轮转）
│
├── page/                               # 页面对象层（Page Object Model）
│   ├── __init__.py                     # 包标识文件（空）
│   ├── page01_login.py                 # P1 - 登录页面
│   ├── page02_search_goods.py          # P2 - 商品搜索页
│   ├── page03_goods_show.py            # P3 - 商品展示页
│   ├── page04_goods_info.py            # P4 - 商品详情页
│   └── page05_goto_pay.py              # P5 - 购物车结算页
│
├── script/                             # 测试用例层（pytest 测试入口）
│   ├── __init__.py                     # 包标识文件（空）
│   ├── test_login.py                   # 登录功能参数化测试
│   └── test_main.py                    # 端到端购物流程测试
│
├── mes/                                # CI 辅助脚本（非测试代码）
│   ├── generate_email.py               # 生成 HTML 邮件正文
│   ├── send_email.py                   # 通过 SMTP 发送邮件
│   └── test_email.py                   # 邮件生成逻辑验证脚本
│
├── report/                             # Allure 原始报告数据（自动生成）
├── config.py                           # 全局配置（URL、路径）
├── conftest.py                         # pytest 共享 Fixture
├── pytest.ini                          # pytest 运行配置
├── requirements.txt                    # Python 依赖包列表（50 个包）
└── 项目搭建说明文档.md                  # 本文档
```

> **注意：** `script/`（单数）是测试用例目录，被 `pytest.ini` 的 `testpaths = script` 指定为测试入口；`mes/` 是 CI 辅助脚本目录，存放邮件生成/发送等非测试脚本。

### 分层架构说明

本项目采用经典的三层架构：

```
┌─────────────────────────────────────────────────────────┐
│               测试用例层 (script/)                        │
│    test_login.py          test_main.py                  │
│         ↑ 调用                ↑ 调用                     │
├─────────────────────────────────────────────────────────┤
│              页面对象层 (page/)                           │
│  Page1Login → SearchGoods → GoodsShow → Page04 → Page05 │
│         ↑ 继承 / 实例化       ↑ 使用工具                  │
├─────────────────────────────────────────────────────────┤
│              公共工具层 (common/)                         │
│    read_login_json.py      log_config.py                │
│         ↑ 读取                ↑ 配置                     │
├─────────────────────────────────────────────────────────┤
│    config.py      data/        pytest.ini   conftest.py │
│   (配置中心)     (测试数据)    (运行配置)    (测试固件)    │
├─────────────────────────────────────────────────────────┤
│              CI 辅助层 (mes/)                             │
│    generate_email.py      send_email.py                 │
│         ↑ 生成邮件            ↑ 发送邮件                  │
├─────────────────────────────────────────────────────────┤
│              CI/CD 层 (.github/workflows/)               │
│    ci.yml — 自动测试 → 生成报告 → 邮件通知               │
└─────────────────────────────────────────────────────────┘
```

---

## 4. 快速开始

### 4.1 克隆项目

```bash
git clone git@github.com:6789y/crmeb_UI.git
cd crmeb
```

### 4.2 创建虚拟环境 & 安装依赖

```bash
# 创建虚拟环境
python -m venv venv

# 激活虚拟环境（Windows）
venv\Scripts\activate

# 激活虚拟环境（macOS/Linux）
source venv/bin/activate

# 安装项目依赖
pip install -r requirements.txt

# 安装 Playwright 浏览器内核
playwright install chromium
```

### 4.3 运行测试

```bash
# 运行所有测试
pytest

# 运行指定测试文件
pytest script/test_login.py
pytest script/test_main.py
```

### 4.4 生成 Allure 报告

```bash
# 生成 HTML 报告
allure generate report -o allure-report --clean

# 打开报告
allure open allure-report
```

---

## 5. 核心配置文件

### 5.1 `config.py` — 全局配置

```python
import os

URL='https://demo25.crmeb.net/'
BASE_PATH = os.path.dirname(os.path.abspath(__file__))
```

| 变量 | 值 | 说明 |
|------|-----|------|
| `URL` | `https://demo25.crmeb.net/` | CRMEB 演示站地址，所有测试的起始页面 |
| `BASE_PATH` | 项目根目录绝对路径 | 用于构建其他文件的绝对路径（如日志文件、数据文件路径） |

### 5.2 `pytest.ini` — pytest 运行配置

```ini
[pytest]
addopts = -s
;          --headed
          --slowmo 1000
;          --tracing on
;          --video on
          #生成测试数据在report目录下allure generate report -o allure-report --clean,

          --alluredir=report
testpaths = script
python_files = test_*.py
python_classes = Test*
python_functions = test_*
```

**配置说明：**

| 参数 | 状态 | 说明 |
|------|------|------|
| `-s` | ✅ 启用 | 关闭捕获 stdout/stderr，允许在控制台看到 print 输出 |
| `--headed` | ❌ 已注释 | 以有头模式运行浏览器（当前注释掉，默认无头模式） |
| `--slowmo 1000` | ✅ 启用 | 每次操作后延迟 1000ms（1秒），便于观察测试过程 |
| `--tracing on` | ❌ 已注释 | 启用追踪（调试用，当前关闭） |
| `--video on` | ❌ 已注释 | 启用录像（当前关闭） |
| `--alluredir=report` | ✅ 启用 | 将 Allure 报告数据输出到 `report/` 目录 |
| `testpaths = script` | ✅ 启用 | 指定测试用例所在的目录 |
| `python_files = test_*.py` | ✅ 启用 | 匹配以 `test_` 开头的 Python 文件 |
| `python_classes = Test*` | ✅ 启用 | 匹配以 `Test` 开头的类 |
| `python_functions = test_*` | ✅ 启用 | 匹配以 `test_` 开头的函数/方法 |

> **注意：** `--headed` 在 `pytest.ini` 中已被注释掉（使用 `;` 注释），因此默认以**无头模式**运行。如需有头模式，请在命令行中添加 `--headed` 参数：`pytest --headed`。

### 5.3 `conftest.py` — 共享 Fixture

> `conftest.py` 是 pytest 的特殊文件，其中定义的 Fixture 会自动被同目录及子目录下的所有测试文件共享使用，无需手动 import。

```python
import pytest
from playwright.async_api import Page
from config import URL


@pytest.fixture
def get_page_func(page: Page):
    """获取page对象"""
    page.goto(URL)  # 开页面
    yield page  # 返回 page 对象
    page.close()  # 关闭页面

@pytest.fixture
def get_alert(page: Page):
    """获取弹窗操作对象"""
    class AlertHelper:
        def __init__(self, page):
            self.page = page
        def text(self):
            """获取弹窗文本内容"""
            return self.page.locator(
                'body > div.el-message.el-message--error.el-message-fade-leave-active.el-message-fade-leave-to > p'
            ).text_content()
    return AlertHelper(page)
```

**Fixture 说明：**

| Fixture | 返回值 | 作用域 | 用途 |
|---------|--------|--------|------|
| `get_page_func` | `page` 对象 | function | 打开目标 URL，返回 Playwright 的 Page 对象供测试使用 |
| `get_alert` | `AlertHelper` 对象 | function | 封装错误弹窗的文本获取操作，用于断言错误提示 |

> **注意：** `conftest.py` 中使用了 `playwright.async_api.Page` 作为类型注解，但 pytest-playwright 的 `page` fixture 实际提供的是同步 API 对象。这在运行时不会报错（因为类型注解不影响运行），但建议统一使用 `sync_api`。

### 5.4 `requirements.txt` — 依赖管理

项目共有 **50 个依赖包**，以下按功能分类列出核心依赖：

#### 核心测试依赖

| 包名 | 版本 | 用途 |
|------|------|------|
| `playwright` | 1.60.0 | 浏览器自动化框架核心库 |
| `pytest` | 9.0.3 | 测试框架 |
| `pytest-playwright` | 0.8.0 | pytest 与 Playwright 的集成插件 |
| `allure-pytest` | 2.16.0 | Allure 报告生成插件 |
| `allure-python-commons` | 2.16.0 | Allure 公共库 |
| `pytest-xdist` | 3.8.0 | 分布式测试执行（并行） |
| `pytest-rerunfailures` | 16.3 | 失败用例自动重跑 |
| `pytest-html` | 4.2.0 | HTML 格式测试报告 |
| `pytest-timeout` | 2.4.0 | 测试超时控制 |
| `pytest-base-url` | 2.1.0 | 基础 URL 配置 |
| `pytest-metadata` | 3.1.1 | 测试元数据 |

#### 其他依赖

| 包名 | 版本 | 用途 |
|------|------|------|
| `selenium` | 4.44.0 | Web 自动化（备用） |
| `Appium-Python-Client` | 5.3.1 | 移动端自动化（备用） |
| `requests` | 2.34.2 | HTTP 请求库 |
| `Flask` | 3.1.3 | Web 框架（备用） |
| `python-dotenv` | 1.2.2 | 环境变量管理 |
| `webdriver-manager` | 4.1.2 | WebDriver 管理（Selenium） |

> **说明：** `requirements.txt` 中包含了一些当前项目未直接使用的依赖（如 `selenium`、`Appium-Python-Client`、`Flask`），这些可能是为未来扩展或其他测试场景预留的。

---

## 6. 公共工具模块（common/）

### 6.1 `common/read_login_json.py` — JSON 数据读取工具

```python
import json
from config import BASE_PATH


def get_test_login_data():
    with open(f'{BASE_PATH}/data/data_login.json', 'r', encoding='utf-8') as f:
        data = json.load(f)
        data_list = []
        for i in data:
            data_list.append((i.get('username'),
                             i.get('password'),
                              i.get('expect'),
                              i.get('type')))
    return data_list
```

**功能：** 从 `data/data_login.json` 读取登录测试数据，返回元组列表，供 `@pytest.mark.parametrize` 参数化使用。

**返回数据格式：**

```python
[
    ('19044972664', '12345678', 'CRMEB', 'pass'),
    ('19044972664', '', '请填写密码', 'fail'),
    ('', '12345678', '请填写手机号', 'fail'),
    ('12', '12345678', '手机号', 'fail'),
    ('122', '12345678', '手机号', 'fail'),
    ('312', '12345678', '手机号', 'fail'),
]
```

### 6.2 `common/log_config.py` — 日志配置工具

```python
import logging.handlers
from config import BASE_PATH


def log_config():
    """日志配置"""
    logger = logging.getLogger()
    if logger.handlers:
        logger.handlers.clear()
    logger.setLevel(logging.INFO)
    sh = logging.StreamHandler()
    fh = logging.handlers.RotatingFileHandler(
        filename=f'{BASE_PATH}/log/log_test.log',
        maxBytes=1024*1024*6,
        backupCount=3,
        encoding='utf-8'
    )
    fmt = '[%(asctime)s] %(filename)s [line:%(lineno)d] %(levelname)s: %(message)s'
    formatter = logging.Formatter(fmt)
    fh.setFormatter(formatter)
    sh.setFormatter(formatter)
    logger.addHandler(fh)
    logger.addHandler(sh)
    return logger
```

**日志配置说明：**

| 配置项 | 值 | 说明 |
|--------|-----|------|
| 日志级别 | `INFO` | 输出 INFO 及以上级别的日志 |
| 控制台输出 | `StreamHandler` | 同时输出到控制台 |
| 文件输出 | `RotatingFileHandler` | 输出到 `log/log_test.log` |
| 单文件最大 | 6 MB | 超过后自动轮转 |
| 保留文件数 | 3 个 | 保留最近 3 个日志文件 |
| 日志格式 | `[时间] 文件名 [行号] 级别: 消息` | 标准日志格式 |

**日志格式示例：**

```
[2026-06-12 20:06:57,682] page01_login.py [line:39] INFO: 输入手机号19044972664
[2026-06-12 20:06:58,708] page01_login.py [line:44] INFO: 输入密码12345678
```

---

## 7. 测试数据（data/）

### `data/data_login.json` — 登录测试数据

以 JSON 格式存储登录测试的测试数据，包含 **6 个测试场景**：

| 场景 | type | username | password | expect | 说明 |
|------|------|----------|----------|--------|------|
| ✅ 正常登录 | `pass` | 19044972664 | 12345678 | CRMEB | 正确手机号和密码，预期页面标题包含 "CRMEB" |
| ❌ 密码为空 | `fail` | 19044972664 | (空) | 请填写密码 | 不输入密码，预期弹窗提示 "请填写密码" |
| ❌ 手机号为空 | `fail` | (空) | 12345678 | 请填写手机号 | 不输入手机号，预期弹窗提示 "请填写手机号" |
| ❌ 手机号过短 | `fail` | 12 | 12345678 | 手机号 | 输入过短的手机号，预期弹窗提示包含 "手机号" |
| ❌ 手机号过短 | `fail` | 122 | 12345678 | 手机号 | 输入过短的手机号，预期弹窗提示包含 "手机号" |
| ❌ 手机号格式错误 | `fail` | 312 | 12345678 | 手机号 | 输入格式错误的手机号，预期弹窗提示包含 "手机号" |

**数据字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `type` | string | 测试场景类型：`pass`（预期成功）/ `fail`（预期失败） |
| `username` | string | 登录手机号 |
| `password` | string | 登录密码 |
| `expect` | string | 预期结果断言文本 |

**优势：** 将测试数据与代码分离，修改测试数据无需修改代码，便于扩展和维护。

---

## 8. 页面对象层（page/）

页面对象层（Page Object Layer）是整个自动化框架的核心。每个页面对象类封装了一个页面的**元素定位**和**操作**，测试用例通过调用页面对象的方法来完成业务操作。

### 设计原则

- 将元素定位器定义为类常量（全大写命名）
- 将操作封装为类方法
- 提供组合方法（如 `login_method()`）供测试用例直接调用
- 通过 `page` 对象与浏览器交互

### 页面对象总览

| 页面类 | 文件 | 对应页面 | 主要操作方法 |
|--------|------|---------|-------------|
| `Page1Login` | `page01_login.py` | 登录页面 | `login_method(username, password)` — 一键登录 |
| `SearchGoods` | `page02_search_goods.py` | 首页搜索区域 | `input_goods()` / `click_goods_search()` |
| `GoodsShow` | `page03_goods_show.py` | 搜索结果列表 | `goods_show()` — 点击第一个商品 |
| `Page04GoodsInfo` | `page04_goods_info.py` | 商品详情页 | `get_coupon()` / `size_choose_a()` / `num_add()` / `num_alter()` / `cart_add()` / `cart_enter()` |
| `Page05GotoPay` | `page05_goto_pay.py` | 购物车页面 | `goto_pay()` — 去结算 |

### 8.1 `page/page01_login.py` — 登录页面对象

**元素定位常量：**

```python
LOGIN_BTN = ('link', '登录/注册')
USERNAME_LOGIN_CLICK = '#__layout > div > div > div.loginBg.min_wrapper_1200 > div:nth-child(1) > div.fastLogin.font-color'
PHONE_INPUT = ('textbox', '请输入手机号')
PASSWORD_INPUT = ('textbox', '请输入密码')
```

**操作方法：**

| 方法 | 功能 |
|------|------|
| `find_by_role(role, name)` | 通过 ARIA role 定位元素（辅助方法） |
| `find_locator(locator)` | 通过 CSS 选择器定位元素（辅助方法） |
| `login_btn()` | 点击「登录/注册」按钮 |
| `username_login_click()` | 点击「用户登录」标签 |
| `phone_input(phone)` | 输入手机号（记录日志） |
| `password_input(password)` | 输入密码（记录日志） |
| `contract_login_click()` | 勾选「同意政策」复选框 |
| `login_click()` | 点击「登录」按钮 |
| `login_method(username, password)` | **组合方法** — 完整登录流程 |

### 8.2 `page/page02_search_goods.py` — 商品搜索页面对象

**元素定位常量：**

```python
SEARCH_GOODS = ('textbox', '搜索商品')
```

**操作方法：**

| 方法 | 功能 |
|------|------|
| `input_goods(goods)` | 输入搜索关键词 |
| `click_goods_search()` | 点击「搜索」按钮 |

> **注意：** 此文件从 `playwright.sync_api` 导入 `Page`，而其他页面类从 `playwright.async_api` 导入。建议统一使用 `sync_api`（pytest-playwright 的 `page` fixture 是同步 API）。

### 8.3 `page/page03_goods_show.py` — 商品展示页面对象

**操作方法：**

| 方法 | 功能 |
|------|------|
| `goods_show()` | 点击搜索结果中的第一个商品 |

### 8.4 `page/page04_goods_info.py` — 商品详情页面对象

**操作方法：**

| 方法 | 功能 |
|------|------|
| `get_coupon()` | 点击领取优惠券 |
| `size_choose_a()` | 选择商品规格 A |
| `num_add()` | 商品数量加 1 |
| `num_alter(num)` | 修改商品数量为指定值 |
| `cart_add()` | 点击「加入购物车」按钮 |
| `cart_enter()` | 点击购物车入口图标 |

### 8.5 `page/page05_goto_pay.py` — 购物车结算页面对象

**操作方法：**

| 方法 | 功能 |
|------|------|
| `goto_pay()` | 点击「去结算」按钮 |

### 页面对象设计模式的优势

- ✅ **可维护性高：** 页面元素变更只需修改对应的页面类，不影响测试用例
- ✅ **代码复用：** 相同的页面操作可在多个测试用例中复用
- ✅ **可读性强：** 测试用例代码清晰地表达了业务操作流程
- ✅ **低耦合：** 页面对象与测试逻辑分离

---

## 9. 测试用例层（script/）

### 9.1 `script/test_login.py` — 登录功能参数化测试

```python
import allure
import pytest
from common.read_login_json import get_test_login_data
from page.page01_login import Page1Login


class TestLogin:

    @pytest.mark.parametrize('username,password,expect,type', get_test_login_data())
    def test_login(self, get_page_func, username, password, expect, get_alert, type):
      with allure.step('登录测试'):
        self.page01_login = Page1Login(get_page_func)
        self.page01_login.login_method(username, password)
        if type=='pass':
            get_page_func.wait_for_timeout(10000)
            title = get_page_func.title()
            assert expect in title
        elif type=='fail':
            msg=get_alert.text()
            assert expect in msg
```

**测试内容详解：**

- **测试类：** `TestLogin`
- **测试方法：** `test_login()`
- **参数化：** 使用 `@pytest.mark.parametrize` 配合 `get_test_login_data()` 从 JSON 文件读取 6 组测试数据
- **Allure 步骤：** 使用 `allure.step('登录测试')` 标记测试步骤
- **断言策略：**
  - **成功场景 (`type='pass'`)：** 执行登录 → 等待 10 秒页面加载 → 获取页面标题 → 断言标题包含预期文本
  - **失败场景 (`type='fail'`)：** 执行登录（错误输入） → 获取弹窗文本 → 断言弹窗包含预期错误信息

### 9.2 `script/test_main.py` — 端到端购物流程测试

```python
from page.page01_login import Page1Login
from page.page02_search_goods import SearchGoods
from page.page03_goods_show import GoodsShow
from page.page04_goods_info import Page04GoodsInfo
from page.page05_goto_pay import Page05GotoPay


class TestMain:
    """测试类"""
    def test_main(self, get_page_func):
        """实例化测试方法"""
        self.page1_login = Page1Login(get_page_func)
        self.page2_search_goods = SearchGoods(get_page_func)
        self.page03_goods_show = GoodsShow(get_page_func)
        self.page04_goods_info = Page04GoodsInfo(get_page_func)
        self.page05_goto_pay = Page05GotoPay(get_page_func)

        # 登录成功
        self.page1_login.login_method('19044972664', '12345678')
        # 输入商品
        self.page2_search_goods.input_goods('鲜花')
        # 点击搜索
        self.page2_search_goods.click_goods_search()
        # 点击商品
        self.page03_goods_show.goods_show()
        # 选择商品规格A
        self.page04_goods_info.size_choose_a()
        # 商品数量加
        self.page04_goods_info.num_alter('2')
        # 加入购物车
        self.page04_goods_info.cart_add()
        # 进入购物车
        self.page04_goods_info.cart_enter()
        # 去结算
        self.page05_goto_pay.goto_pay()
```

**测试内容详解：**

- **测试类：** `TestMain`
- **测试方法：** `test_main()`
- **测试流程：** 登录 → 搜索「鲜花」 → 点击商品 → 选择规格 → 修改数量为 2 → 加入购物车 → 进入购物车 → 去结算
- **特点：** 一次性完成整个购买流程，使用固定测试账号

---

## 10. CI 辅助脚本（mes/）

`mes/` 目录存放 CI 流水线中使用的辅助脚本，主要用于生成和发送测试报告邮件。

### 10.1 `mes/generate_email.py` — 生成邮件 HTML 内容

**功能：** 从环境变量中读取测试结果信息，生成格式化的 HTML 邮件正文。

**读取的环境变量：**

| 环境变量 | 说明 |
|----------|------|
| `BRANCH` | Git 分支名 |
| `COMMIT_HASH` | 提交哈希（截取前 8 位） |
| `COMMIT_AUTHOR` | 提交者 |
| `COMMIT_MSG` | 提交信息 |
| `TEST_STATUS` | 测试状态（`COMPLETED` / `FAILED`） |
| `TEST_TIME` | 测试时间 |
| `GITHUB_REPOSITORY` | GitHub 仓库（如 `6789y/crmeb_UI`） |
| `GITHUB_SERVER_URL` | GitHub 服务器 URL |
| `GITHUB_RUN_ID` | GitHub Actions 运行 ID |
| `TEST_TOTAL` | 用例总数 |
| `TEST_PASSED` | 通过数 |
| `TEST_FAILED` | 失败数 |
| `TEST_SKIPPED` | 跳过数 |
| `TEST_ERRORS` | 错误数 |

**邮件内容包含：**

- 测试状态徽章（✅ 通过 / ❌ 失败 / ⚠️ 未知）
- 分支、提交、触发者、测试时间信息表
- 测试结果统计卡片（总计/通过/失败/跳过/错误）
- 通过率进度条
- 提交信息
- 「查看详细报告」按钮（链接到 GitHub Actions 运行页面）

**输出：** 将 HTML 内容写入 `email_body.html` 文件。

### 10.2 `mes/send_email.py` — 发送测试报告邮件

**功能：** 从环境变量读取邮箱配置和邮件 HTML 内容，通过 SMTP 发送邮件。

**读取的环境变量：**

| 环境变量 | 说明 |
|----------|------|
| `MAIL_USERNAME` | 发件人邮箱地址 |
| `MAIL_PASSWORD` | 邮箱 SMTP 授权码 |
| `MAIL_TO` | 收件人邮箱（默认发送给发件人自己） |
| `BRANCH` | Git 分支名（用于邮件主题） |
| `TEST_STATUS` | 测试状态（用于邮件主题） |
| `TEST_TOTAL` | 用例总数（用于邮件主题） |
| `TEST_PASSED` | 通过数（用于邮件主题） |

**支持的邮箱类型：**

| 邮箱 | SMTP 服务器 | 端口 | 加密方式 |
|------|------------|------|----------|
| QQ 邮箱 | `smtp.qq.com` | 465 | SSL |
| 163 邮箱 | `smtp.163.com` | 465 | SSL |
| Gmail | `smtp.gmail.com` | 587 | STARTTLS |
| Outlook/Hotmail | `smtp.office365.com` | 587 | STARTTLS |
| 其他（默认 QQ） | `smtp.qq.com` | 465 | SSL |

**邮件主题格式：**

```
✅ 通过 CRMEB UI 自动化测试报告 - main (5/6 83.3%)
❌ 失败 CRMEB UI 自动化测试报告 - main (3/6 50.0%)
```

### 10.3 `mes/test_email.py` — 邮件生成验证脚本

**功能：** 临时测试脚本，验证 `generate_email.py` 是否能正确生成邮件 HTML。

**工作流程：**
1. 设置测试环境变量
2. 执行 `generate_email.py`
3. 验证 `email_body.html` 是否生成
4. 检查 HTML 内容是否包含预期元素
5. 清理临时文件
6. 将验证结果写入 `test_verify_result.txt`

---

## 11. GitHub Actions CI/CD

### 11.1 CI 配置文件

**文件路径：** `.github/workflows/ci.yml`

### 11.2 触发条件

| 触发方式 | 条件 |
|----------|------|
| `push` | 推送到 `main`、`master`、`develop` 分支 |
| `pull_request` | PR 到 `main`、`master` 分支 |
| `workflow_dispatch` | 手动触发（可指定收件人邮箱） |

### 11.3 全局环境变量

| 变量 | 来源 | 说明 |
|------|------|------|
| `TZ` | 固定值 | 时区：`Asia/Shanghai` |
| `MAIL_USERNAME` | GitHub Secrets | 发件人邮箱 |
| `MAIL_PASSWORD` | GitHub Secrets | SMTP 授权码 |
| `MAIL_TO` | GitHub Secrets | 收件人邮箱 |

### 11.4 CI 流水线步骤（共 15 步）

| 步骤 | 名称 | 说明 |
|------|------|------|
| 1 | 检出代码 | `actions/checkout@v4`，`fetch-depth: 0` |
| 2 | 设置 Python | `actions/setup-python@v5`，Python 3.10 |
| 3 | 缓存 pip 依赖 | `actions/cache@v4`，按 `requirements.txt` 哈希缓存 |
| 4 | 安装 Python 依赖 | `pip install -r requirements.txt` |
| 5 | 安装 Playwright 浏览器 | `playwright install chromium` + `install-deps` |
| 6 | 记录测试开始时间 | 输出时间戳到 `$GITHUB_OUTPUT` |
| 7 | 执行测试并生成 Allure 报告 | `pytest` 命令，`continue-on-error: true` |
| 8 | 检查测试结果 | 从日志提取用例统计（passed/failed/skipped/errors） |
| 9 | 生成 Allure HTML 报告 | `allure generate`，自动安装 Allure CLI |
| 10 | 上传 Allure 报告产物 | `actions/upload-artifact@v4`，保留 7 天 |
| 11 | 上传原始报告数据 | `actions/upload-artifact@v4`，保留 7 天 |
| 12 | 上传测试日志 | `actions/upload-artifact@v4`，保留 7 天 |
| 13 | 生成邮件 HTML 内容 | 执行 `mes/generate_email.py` |
| 14 | 发送测试报告邮件 | 执行 `mes/send_email.py` |
| 15 | 任务状态总结 |